""" This is an __init__ file. It turns the websocket folder into a python 
    package and can do some initialization (such as adding to the python path)
    before invoking the other scripts in this package """

import sys

sys.path.append('lib')

